<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.alex.wingshooterspocketapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <!-- Set default activity for launch-->
        <activity
            android:name=".LogRegMainActivity"
            android:theme="@style/AppTheme">
                <intent-filter>
                    <action android:name="android.intent.action.MAIN"/>
                    <category android:name="android.intent.category.LAUNCHER"/>
                </intent-filter>
        </activity>
        <!-- launch activities -->
        <activity android:name=".Home_Screen"/>
        <activity android:name=".LoginRegister"/>
        <activity android:name=".gamebirdidmain"/>
        <activity android:name=".hsprovincedates"/>
        <activity android:name=".HSProvinceSelection"/>
        <activity android:name=".LogActivity"/>
        <activity android:name=".NewHunt"/>
        <activity android:name=".StartHunt"/>

    </application>

</manifest>